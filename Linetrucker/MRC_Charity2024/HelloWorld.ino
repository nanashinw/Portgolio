  // tfade(325,40,90,0.1);
  // curve(L, 400);
  // tfade(550,40,90,0.1);
  // curve(L, 400);
  // tfade(900,40,90,0.1);
  // curve(R, 400);
  // tfade(600,40,90,0.1);
  // curve(L, 300);
  // tfade(550,40,100,0.1);
  
  // curve(R, 400);
  // ttime(40,0.017,0.1, 1100);

  // tfade(500,40,100,0.1);
  // curve(R, 400);
  // tfade(700,40,100,0.1);
  // curve(L, 400);
  // tfade(950,40,100,0.1);
  // curve(R, 400);
  // tfade(1200,35,90,0.1);
  // curve(L, 400);

  // //phase 2
  // tfade(250,40,100,0.1);
  // curve(L, 400);
  // ttime(40,0.015,0.05,2300);
  // curve(L, 200);
  // tfade(150,40,100,0.1);
  // curve(R, 400);
  // tfade(400,40,100,0.1);
  // curve(R, 400);
  // ttime(40,0.015,0.05,900);
  // tfade(500,40,100,0.1);

  // curve(L, 400);
  // tfade(200,40,100,0.1);
  // curve(R, 200);
  // tfade(200,40,100,0.1);
  // curve(R, 200);
  // tfade(100,40,100,0.1);
  // t(40,0.017,0.1);
  // t(40,0.017,0.1);
  // l();
  // tfade(300,40,100,0.1);
  // curve(R, 200);
  // ttime(40,0.015,0.05,1200);
  // tfade(900,40,100,0.1);
  // curve(L, 400);
  // tfade(700,40,100,0.1);
  // curve(L, 2000);



  